# 면허시험 문제은행 갱신 가이드 (사용자분 전용)

이 문서는 한국도로교통공단에서 새로운 학과시험 PDF가 배포되었을 때, 이 사이트의 데이터를 새 PDF 기준으로 갱신하는 절차를 한국어로 설명한 매뉴얼입니다.

## 🎯 결론부터 — 갱신은 "PDF만 다운받으면 끝" (+ GPTs 재업로드 1분)

새 PDF가 도착하면 사용자분이 하실 일은 **딱 세 가지**:
1. **PDF 4개 파일을 `원본_PDF/` 폴더에 넣기** (덮어쓰기 OK)
2. **Claude에게 "갱신 시작하자" 한마디**
3. **PR 머지 후, ChatGPT GPTs Knowledge 에 새 `quiz_bank_*.txt` 4파일 재업로드** (UI 작업 1분, Claude 안내)

그 다음은 Claude가 자동화 스크립트를 돌리고, 매 단계 결과를 보여드립니다. 사용자분은 **확인·승인·머지·GPTs 재업로드**만 하시면 됩니다.

---

## 📋 사용자분이 준비해야 할 것 (Claude에게 갱신 시작 전)

### 필수
- [ ] **새 PDF 4개**: 한국어·영어·중국어·베트남어 (도로교통공단 배포본)
- [ ] PDF 파일명에 다음 키워드 중 하나 포함: `한국어`, `English`, `Chinese`, `Vietnamese`
  - 예: `(1,2종) 학과시험 문제은행[한국어]_(2027.3.9.시행).pdf`
  - 자동 식별 가능한 형식이라 사용자가 직접 매핑 안 해도 됨
- [ ] 기존 `원본_PDF/` 폴더의 옛 PDF는 백업하거나 덮어쓰기 (Claude가 안내)

### 선택 (이미지 갱신 필요 시)
- [ ] 새 PDF에 사진형·표지판형 이미지가 변경됐는지 (대부분 그대로)
- [ ] 변경됐다면 Claude가 이미지 위치 매칭으로 자동 추출

---

## 🚦 갱신 절차 (Claude와 함께)

### 1단계: 안전 백업 (Claude 자동)
- 현재 `index.html`, `en.html`, `cn.html`, `vn.html` 의 백업 사본 (`*.bak`)
- 작업 전 GitHub `main` 브랜치 좌표를 백업 브랜치로 보존 (예: `backup-before-refresh-2027-03-15`)
- 작업은 별도 브랜치 (예: `refresh-2027`)에서 진행

**사용자분은 그냥 보고만 받으시면 됩니다.**

### 2단계: PDF 자동 추출 (Claude 자동)
```powershell
python scripts/refresh_pdf.py
```
- 4개 PDF에서 1000개씩 자동 파싱
- 현재 HTML과 비교 → 추가·삭제·변경 번호 자동 식별

**사용자분 확인 사항** (Claude가 보고드림):
- PDF에 1000개가 모두 추출됐는지
- HTML에 추가될 번호 (예: "Q15, Q1001 새로 추가됨")
- HTML에서 사라질 번호 (예: "Q200 폐기됨")

여기서 사용자분이 **승인**해주시면 다음 단계.

### 3단계: HTML 갱신 (Claude 자동)
- 추출된 데이터로 4개 HTML의 객체 배열 갱신
- 카운터 자동 재계산
- 새 객체의 유형(t) 자동 추론 (사진/표지판/문장/동영상형)

### 4단계: 이미지 갱신 (필요 시)
새 PDF에 새 이미지가 들어온 경우 Claude가 보고:
- "Q1001은 사진형인데 imgs 빈 칸. 페이지 X 위/중간/아래 그림 중 어느 것입니까?"
- 사용자분이 PDF 보고 확인 → Claude가 자동 추출·저장·HTML 반영

```powershell
python scripts/match_images.py --pdf ko --num 1001 --num 1002
```

### 5단계: GPTs 학습 텍스트 동기화 (Claude 자동)
- HTML 갱신이 끝나면 Claude가 자동으로:
```powershell
py html_to_txt.py
```
- → `gpt_files/quiz_bank_{ko,en,cn,vn}.txt` 4파일 재생성 (한국어 해설·이미지를 비한국어에도 자동 채움)
- 검증: 4파일 모두 문항=1000, 동영상형=35, 해설≈996, 이미지=285 일치 확인
- **이 4파일은 HTML과 같은 PR에 함께 포함** (별도 PR 만들지 않음)

### 6단계: 검증 + PR
- Claude가 자동 검증: 4개 언어 카운트 일관성, 정답 형식, 데이터 무결성, gpt_files 동기화 상태
- 모두 통과하면 PR 생성 (예: `refresh-2027`)
- 사용자분에게 PR URL 전달 → **사용자분이 GitHub에서 검토 후 머지**

### 7단계: 바탕화면 + GPTs 재업로드 (Claude 안내 + 사용자분 수행)
- 머지 후 Claude가 바탕화면(`C:\Users\user\OneDrive\바탕 화면\`)에 새 4파일 자동 복사
- 사용자분이 ChatGPT GPTs UI에서 (1~2분):
  1. ChatGPT → 본인 GPTs 편집 화면 → Configure → Knowledge
  2. 기존 `quiz_bank_{ko,en,cn,vn}.txt` 4파일 **삭제**
  3. 바탕화면의 새 4파일 **업로드** → **Save**
- 검증 프롬프트로 확인: "966번 해설해줘" / "Explain question 970" / "Câu 1000 giải thích"

### 8단계: 사이트 검증 (사용자분)
- GitHub Pages 빌드 (2~3분) 후 https://evergreenedu-collab.github.io/quiz/ 접속
- 새 문항·정답·이미지 정상 표시 확인
- 문제 발견 시 Claude에게 알림 → revert 가능

---

## 🤔 자주 묻는 질문

### Q. 갱신할 때마다 Claude에게 같은 설명을 반복해야 하나요?
A. **아니요.** 이 폴더의 `CLAUDE.md` 파일에 작업 패턴이 모두 적혀 있어, Claude가 이 프로젝트에서 작업하면 자동으로 참조합니다. **"갱신 시작하자"** 한마디면 충분합니다.

### Q. 작업하다 잘못되면 어떻게 하나요?
A. 4중 안전장치:
1. 로컬 `.bak` 사본 — 단일 파일 즉시 복원
2. 로컬 폴더 통째 백업 — 전체 복원
3. GitHub 백업 브랜치 — `main`을 작업 전 상태로 force reset (Claude가 사용자분 승인 받고)
4. PR revert — 머지 후 문제 발견 시 새 PR로 되돌림

### Q. PDF에서 자동 추출이 정확한가요?
A. 이미 검증된 알고리즘 (PR #4·#6 작업 기준):
- 한국어·영어·베트남어 1000개 정확 추출
- 중국어 PDF 오타 케이스(점 없는 Q455 같은 것)도 처리
- 정답 라벨 차이(베트남어 `Đáp án đúng :`)도 자동 처리

### Q. 새 PDF가 형식이 약간 바뀌면 어떻게 하나요?
A. Claude에게 "새 PDF 형식이 다른 것 같아"라고 알려주시면, Claude가 PDF를 분석해 추출 알고리즘을 조정합니다. 핵심 패턴(`■ 정답`, `①②③④` 등)이 같다면 거의 자동.

### Q. 동영상은 어떻게 갱신하나요?
A. 새 동영상이 추가되면:
1. 사용자분이 새 영상을 `movie/` 폴더에 두기
2. Claude가 ffmpeg로 mp4 변환 + GitHub Releases 업로드
3. HTML의 영상 매핑 갱신
4. 이건 별도 명령 — "동영상도 갱신해야 해" 라고 알려주시면 Claude가 절차 안내.

### Q. AI 튜터에 새 기능 추가하고 싶으면?
A. CLAUDE.md 와 분리된 별도 작업입니다. Claude에게 "새 기능 추가하자" 같이 자유롭게 요청하시면 됩니다.

---

## 📦 폴더 구조 (사용자분 알아두기)

```
외국인-학과시험-AI튜터/
├── 원본_PDF/                      # 사용자분이 새 PDF 넣는 곳
│   ├── (1,2종) ... [한국어]...pdf
│   ├── (1,2종) ... [English]...pdf
│   ├── (1,2종) ... [Chinese]...pdf
│   └── (1,2종보통) ... [Vietnamese]...pdf
├── scripts/                       # Claude가 자동으로 돌리는 도구
│   ├── refresh_pdf.py             # PDF → HTML 데이터 추출·비교
│   └── match_images.py            # 이미지 자동 매칭·추출
├── images/                        # 사진형·표지판형 그림 (291개)
├── movie/                         # 동영상 35개 (GitHub에 업로드 안 됨)
├── index.html / en.html / cn.html / vn.html  # 4개 언어 학습 페이지
├── CLAUDE.md                      # Claude 자동 참조 (수정 X)
└── UPDATE_GUIDE.md                # 본 가이드 (사용자분용)
```

---

## ✅ 체크리스트 (갱신 시작할 때 사용)

```
[ ] 새 PDF 4개를 원본_PDF/ 폴더에 배치
[ ] 파일명에 한국어/English/Chinese/Vietnamese 키워드 포함 확인
[ ] Claude에게 "갱신 시작하자" 입력
[ ] (Claude 보고) 백업 완료 확인
[ ] (Claude 보고) PDF 추출 결과 확인 → 승인
[ ] (Claude 보고) HTML 갱신 결과 확인
[ ] (필요 시) 이미지 매칭 검증
[ ] (Claude 보고) gpt_files/*.txt 4파일 동기화 완료 (문항=1000, 동영상=35, 해설≈996, 이미지=285)
[ ] PR 검토 + 머지 (HTML + gpt_files 같은 PR)
[ ] (Claude 자동) 바탕화면에 새 4파일 복사 확인
[ ] ChatGPT GPTs Knowledge 에 새 4파일 재업로드 (기존 삭제 → 신규 업로드 → Save)
[ ] GPTs 검증 프롬프트 테스트 ("966번 해설해줘" 등)
[ ] GitHub Pages 빌드 후 사이트 검증
[ ] 일주일 안정 확인 후 백업 자료 정리 (Claude 도와드림)
```

문제가 생기면 언제든지 Claude에게 도움을 요청하세요. 프로젝트 폴더의 `CLAUDE.md` 가 작업 컨텍스트를 자동으로 제공하니 처음부터 다시 설명할 필요 없습니다.
